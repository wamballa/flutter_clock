import 'package:flutter/material.dart';

class Animation_Page extends StatelessWidget {
  const Animation_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    print('ANIMATION');
    return Container(
      color: Colors.pink,
    );
  }
}
